version: "3.2"
services:
  web_crawler:
    container_name: web_crawler
    image: web_crawler
    build: web_crawler
    restart: always
  nginx:
    container_name: nginx
    image: nginx
    build: nginx
    restart: always
    ports:
      - "80:80"
    depends_on:
      - web_crawler
